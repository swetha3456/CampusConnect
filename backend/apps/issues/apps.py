from django.apps import AppConfig


class IssuesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.issues'
# # apps/issues/apps.py
# from django.apps import AppConfig

# class IssuesConfig(AppConfig):
#     name = 'apps.issues'  # Ensure this is correct
